// Compile the Edlib implementation used by the vendored RAD core. R only
// builds translation units directly under src/, while the implementation is
// kept with its upstream headers for provenance.
#include "vendor/rad_core/include/edlib/src/edlib.cpp"
